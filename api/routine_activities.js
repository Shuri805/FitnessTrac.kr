const express = require('express');
const routineActivitesRouter = express.Router();
const {} = require('../db');
const {requireUser} = require('./utils');



module.exports = {
    routineActivitesRouter
}